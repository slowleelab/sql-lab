# SQL Lab 项目指令

## 新增章节时必须全局重编号

每次新增案例/章节，**禁止**只给新章节分配追加式大号（如 113、114、115）而保持其他章节编号不动。新增的同时必须对**全部章节从 1 开始连续重新编号**，让编号体系始终连续。

**编号顺序**：严格按侧边栏分类顺序——
一、索引设计与失效 → 二、查询改写 → 三、JOIN 优化 → 四、DDL 与大表 → 五、架构级优化 → 六、事务与锁 → 七、优化器与 8.0 新特性 → 八、TiDB 分布式优化；
分类内按侧边栏排列顺序编号（新章节先确定插入位置，其后的所有章节顺延）。

**重编号时必须逐项同步更新，漏一项即视为未完成：**

1. **文件名**：`docs/cases/<分类>/NN-*.md`；存在可运行 SQL 时，`sql/cases/NN-*/` 目录名一并改（目录内文件引用同步）。
2. **侧边栏**：`docs/.vitepress/config.ts` 中每条的序号文本（`NN · ...`）与 `link` 路径。
3. **PDF 脚本**：`scripts/generate-pdf.js` 中 `CATEGORIES` 各分类的 `nums` 白名单。
4. **案例索引页**：`docs/cases/index.md` 的 `#` 列编号、链接路径、每个分类标题的"（N 个）"计数、顶部案例总数。
5. **README**：`cases-NN` badge、案例总览的总数与各分类计数、表格链接。
6. **交叉引用**：全仓搜索旧编号（如"案例 114"），更新其他案例正文中的所有引用。
7. **收尾验证**：`npm run docs:build` → `npm run docs:pdf` → 确认 PDF 书签数等于案例总数、编号连续 → 复制 PDF 到 `docs/.vitepress/dist/` → commit & push。

> 说明：现存 115 个案例中 103–115 为历史追加编号，保持现状直到下次新增章节时按本规则一次性全局重排。
